// Clear search box

GitSee.stateObj = { foo: "bar" };
// history.pushState(stateObj, title, url);
history.pushState(GitSee.stateObj, 
                  "<%= @repo.repo_username %>" + ": " + "<%= @repo.repo_name %>" + " " + "documentation", 
                  "/repo/" + "<%= @repo.repo_username %>" + "/" + "<%= @repo.repo_name %>");
$('#repo-title').html('<%= @repo.repo_name %>');
$('.gems').val('');
$('.gems').html("<%= escape_javascript( render partial: "gemms/gemm", collection: @repo.gemms ) %>");

// <<<<<<< HEAD
// // $('.gem-resources').hide();
// =======
// //$('.gem-resources').hide();
// >>>>>>> users

// Setup a click event on the gem, so when they are clicked, they show all gem-resources

// $('.gem-name').on('click', function(){
// 	$('.gem-resources').toggle();
// })